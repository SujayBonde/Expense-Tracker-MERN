{
  "name": "expense-tracker",
  "version": "1.0.0",
  "description": "Expense Tracker MERN App",
  "scripts": {
    "start": "concurrently \"npm run server --prefix backend\" \"npm run client --prefix frontend\"",
    "server": "npm run server --prefix backend",
    "client": "npm run dev --prefix frontend",
    "install-all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
